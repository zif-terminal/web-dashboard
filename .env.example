# Backend URLs (server-side only, used by next.config.ts rewrites)
# For local development:
#   HASURA_URL=http://localhost:8080
#   AUTH_URL=http://localhost:8081
# For remote (default if not set):
#   HASURA_URL=http://167.99.145.4
#   AUTH_URL=http://167.99.145.4
HASURA_URL=http://167.99.145.4
AUTH_URL=http://167.99.145.4

# Frontend endpoints (use the Next.js proxy - no need to change these)
NEXT_PUBLIC_GRAPHQL_ENDPOINT=/api/graphql
NEXT_PUBLIC_AUTH_ENDPOINT=/api/auth
NEXT_PUBLIC_USE_MOCK_API=false

# B4.1: Hasura WebSocket endpoint for real-time run-status subscriptions.
# Set to the Hasura GraphQL WebSocket URL (ws:// or wss://).
# If not set, the UI falls back to 5-second HTTP polling after 10s of disconnect.
# Example (direct to Hasura): NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=ws://167.99.145.4/v1/graphql
# Example (local dev):        NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=ws://localhost:8080/v1/graphql
NEXT_PUBLIC_GRAPHQL_WS_ENDPOINT=
