# web-dashboard (zif-app) — untangle-staging commit manifest

Tracks the source↔deployed mapping for FE bundles staged here for the untangle effort.

| Date (UTC) | Task | Deployed bundle | Summary |
|---|---|---|---|
| 2026-07-10 | #186-follow-on | `index-BF_Wf-S4.js` | Accounts page: wallets now **collapsed by default** (click chevron to expand); always-visible **issue cards** in each wallet header show which accounts have `data_complete=false` ("⚠ Incomplete data") or accuracy=gap/mismatch ("Minor gap"/"Mismatch") so issues are scannable without expanding. Cards name the account (exchange label). QA reallogin PASS. |
| 2026-07-10 | #212-analytics | `index-BsSXlHh-.js` | Analytics consolidation: renamed Performance → **Analytics** (key stays `performance`); removed the standalone **Income** tab and folded its period cards + per-period breakdown into Analytics; added a range selector (since-last-checked / 24h / 7d / 30d / 90d / YTD / All / Custom + Day/Week/Month/Year grain); added an **Exit** column (LIQUIDATED — Lighter + Variational only, no fabricated SL/TP/limit) driven by `mat_closed_trades.is_liquidation`; added an **Open (partially realized)** sub-section (mat_open_lifecycle realized) + an unassociated-income note; per-fill WAC PnL now shows on Activity FILL rows (backend view change; FE already rendered `pnl`); added an Overview **"since you last checked"** pulse strip + persisted `zif.lastChecked` marker. Backend (separate, in infra repo): `mat_income_periods` year grain, `mat_activity_stream` FILL LEFT JOIN position_events, `mat_closed_trades.is_liquidation`. QA reallogin + full PASS (Overview Σequity to the cent, 94 open · 955 closed live). |
| 2026-07-10 | #222-gap-magnitude | `index-DAPOkBEK.js` | Accounts page: **show the reconciliation gap magnitude + a plain-English explanation** before any reconciliation. Backend: added a `gap_amount` column to the `mat_accounts` view (migration `1865000000_mat_accounts_gap_amount`) = the netflow residual `equity − realized − unrealized + net_flow` (the #186 plug figure), sourced from `mat_portfolio`, COALESCE 0 when no portfolio row; added to the user-role SELECT perm + tracked yaml. FE: wired `gap_amount` through `ACCOUNTS_SUB` → `Account.gapAmount`; issue cards now append **"$X off"**; each account's expanded status shows a sign-aware **"$X higher/lower than the exchange balance"** line + a tooltip explaining Reconciled vs Gap (computed == exchange-reported). Read-only / display-only — **no** synthetic_pnl / plug writes. Gap ties to the 4 incomplete accounts (97b8486f ≈ +$86,864.81, 8efca76f ≈ +$37.33, 4aa3dd58 ≈ −$119.10; Variational 6295bf09 has no mat_portfolio row → 0). QA reallogin PASS; gap figures verified rendering live. |
| 2026-07-10 | #223-reconcile-status | `index-CeNl_MPf.js` | **ONE deterministic reconciliation-status rule** (Jaison directive) encoded ONCE in the backend and mapped straight to the UI — no per-agent/ad-hoc thresholds. Backend: added a `reconcile_status` column to the `mat_accounts` view (migration `1866000000_mat_accounts_reconcile_status`): `NOT data_complete → 'incomplete'`; `elif abs(gap_amount) <= 5 → 'reconciled'` (TOL=$5, documented literal `-- reconcile tolerance USD, set by Jaison 2026-07-10`); `else → 'gap'`. Added to the user-role SELECT perm + tracked yaml. FE: `reconcile_status` is now the **SINGLE source** for the badge/color/copy and whether a gap shows — **removed the arbitrary $1 GAP_FLOOR from #222** (the $5 TOL is now the only threshold, in the backend). Issue cards now include BOTH `incomplete` AND `gap` accounts (93a13de9 Lighter shows a **Gap · ~$4,463 off** card; previously only incomplete showed). Mapping: incomplete → "⚠ Incomplete data · $X off"; gap → "⚠ Gap · $X off"; reconciled → "✓ Reconciled" (no gap number). Tooltips preserved (Reconciled = computed == exchange-reported; Gap/Incomplete = off by $X). Wired `reconcile_status` through `ACCOUNTS_SUB` → `Account.reconcileStatus` (+ client fallback mirroring the same rule for mock). Read-only / display-only — **no** synthetic_pnl / reconciliation writes. DB distribution: 27 reconciled · 11 gap · 4 incomplete. QA reallogin PASS; accounts-reconcile223 probe PASS (4 incompletes, 93a13de9 Gap, reconciled ✓ all render live). |

| 2026-07-10 | #223-analytics-rebuild | `index-BkzxM8K3.js` | **Analytics page REBUILT to Jaison's exact spec** — stripped to two things: (A) a **totals header** (Net PnL · Trade PnL · Funding · Fees · Rewards · Interest · Hacks) for the selected range, with presets **1hr / 24h / Week / Month / YTD / All + custom date range**; totals = SUM of each category over the range. (B) a **per-position breakdown list** — COMPLETE (fully-closed) **and** PARTIAL (open with realized from partial closes), sorted by close/last-event date DESC (tiebreak id), **infinite-scroll prefetch at 50%** (IntersectionObserver sentinel at the list's midpoint), range-filtered. Each row: **Wallet · Exchange · Account tags** (reused `lib/tags.tsx`) · earliest→last event date · asset · the 7 sign-aware numbers (`format.ts` dust rules). **Click a row → expand → ALL contributing events** (type · date · amount) DESC by ts. **REMOVED** the old Income-by-period cards, the closed-trades Exit-column list, the partial-closes sub-section, and the unassociated-income note. Backend (infra repo, migration `1867000000_mat_position_breakdown`): new read-only VIEW **`mat_position_breakdown`** — per-position rollup of `position_pnl` (SAME sign convention as `mat_closed_trades`: fees=−fee_pnl, net=trade+funding+interest+rewards+hacks−fees, synthetic excluded) covering `status='closed' OR realized_pnl<>0`; cols id/asset/exch/account/is_partial/earliest_event_ts/last_event_ts(epoch-ms, sort key)/net_pnl + 6 buckets; RLS via `exchange_account→wallet→user_wallets` (per-user wallet label via the relationship, like mat_closed_trades). Plus **`mat_position_events`** — position_id-keyed contributing-event detail (type · ts · signed-USD amount) for the exact expand list. Both tracked + user-role select perm. **RECONCILIATION:** Σ list net == header Net PnL **to the cent** (both sum the SAME view rows over the SAME window; all-time Σ = **$1,023,647.01** == `position_pnl`). NOTE (money-path decision): the header sums this per-position view (position_pnl buckets), NOT `mat_ledger`/`mat_income_periods` — those are a *different* event-level aggregation (they carry the −$342,670 hack as a transfer + standalone fee/interest events) and do NOT tie bucket-for-bucket to position_pnl; summing the same view keeps the header and the list reconciled to the cent, which is the spec's gate. Read-only / display-only — **no** fabricated writes. QA reallogin PASS (1016 positions · 32 partially realized live); analytics223 probe PASS (7 header cards; totals update Month $221.6K→All $1.02M; COMPLETE+PARTIAL sorted DESC; **list Σ reconciles to header, both +$41.2K**; row-expand event detail DESC by ts; no console errors). Screenshots: page + expanded row. |

Prior FE state before this change: `index-CeNl_MPf.js` (#223-reconcile-status).

| 2026-07-10 | #224-copyable-ids + reconcile-wording | `index-CuOJG0fk.js` | Two Accounts-page changes in one deploy. **CHANGE 1 — copyable identifiers:** each expanded account row now shows a 3-field identifier block (Wallet address · Exchange ID · Internal ID) with a copy-to-clipboard button per field ("Copied!" flash on success). Backend: added `account_identifier` (exchange-given address/id from `exchange_accounts.account_identifier`) to the `mat_accounts` view (migration `1868000000_mat_accounts_account_identifier`, CREATE OR REPLACE preserving all existing columns); added to the user-role SELECT perm + tracked yaml + metadata reload. FE: wired `account_identifier` through `ACCOUNTS_SUB` → `Account.accountIdentifier`; also mapped existing `wallet_address` → `Account.walletAddress` (was already in the subscription, now surfaced in the type). **CHANGE 2 — reconcile wording:** updated `RECONCILE_META.detail` and `gapExplain` tooltips — reconciled: detail "Matches the exchange", tooltip "Reconciled — your computed positions & PnL match what the exchange reports for this account."; gap: detail "All trades present, value off", tooltip "All trades present — but our value is $X higher/lower than the exchange shows."; incomplete: detail "Missing trades, value off", tooltip "Missing some trades — our value is $X higher/lower than the exchange shows (may correct once the trades land).". Badge labels (Reconciled/Gap/Incomplete), colors, and $5 TOL unchanged. QA reallogin PASS. |
| 2026-07-10 | #225-accounts-restructure | `index-YAvDyPOI.js` | **Accounts page restructure — Wallet→Exchange→Account hierarchy.** Four changes: (1) **Wallet address moved to wallet header** (shown ONCE, copyable "Address" field) — removed from per-account rows; (2) **Internal ID (zif UUID) removed** from per-account identifier block entirely; (3) **"Exchange ID" relabelled to "Account ID"** — keeps only the exchange-given id (e.g. Lighter 711452, HL address, Variational manual-…) with copy button; (4) **Exchange sub-groups** — accounts under each wallet are now grouped by exchange (colored dot + ALL-CAPS exchange name + count as a sub-header row between the wallet header and its accounts), sorted alphabetically. Reuses `exchMeta` colors/dots; collapse-by-default + issue cards + reconcile badges + tags + api-key states + edit/hide/delete icons all preserved. QA reallogin PASS (login ✓, Analytics 1016 positions live ✓, no console errors ✓; screenshot confirms Wallet→HYPERLIQUID (1)→LIGHTER (3) hierarchy with Address in wallet header, Account ID per-account). |
| 2026-07-10 | #227-accounts-polish | `index-CQa-fdvY.js` | **Accounts page polish — 3 changes.** (1) **Exchange header visual hierarchy:** sub-group header (HYPERLIQUID/LIGHTER + count) now has a subtly brighter background band (`rgba(255,255,255,.028)`) + border-top divider; account rows beneath are indented 12px — wallet→exchange→account reading order is now obvious. (2) **Removed redundant per-account tags:** exchange badge (Hyperliquid/Lighter) and wallet-label chip ("Zif US") removed from account rows — both are already conveyed by the grouping; kept: account-type badge (Main/Sub), user tags, reconcile badge. (3) **Value/PnL labels:** small "Value" / "PnL" labels (muted, 9.5px) added above each number on account rows AND wallet header totals (desktop + mobile). No backend changes. QA: login PASS, no console errors, no GQL field errors, Value/PnL labels confirmed (15 Value + 14 PnL elements detected). |
